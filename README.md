Run ```docker-compose up --build``` and wait for the containers to be running. 
NOTE: This might take some time as there are quite a few packages to download from npm.

Then open ```http://localhost:3000``` in the browser.